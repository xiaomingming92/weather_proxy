# {需求名称} PRD 增量更新 v{版本号}

> PRD 增量模板：用于在已有 PRD 基础上追加/修改/删除需求。不是独立文档，是 PRD 的变更记录。
> 新建 PRD 请使用 `prd-standard-template.md`。

**创建时间**: {ISO 时间戳}
**作者**: {作者标识}
**状态**: 草稿 / 评审中 / 已确认
**基于 PRD**: `docs/{项目}/knowledge/00-需求/{原PRD文件名}.md` v{原版本号}

---

## 变更摘要

| 类型 | 数量 | 说明 |
|------|:--:|------|
| 新增 | N | {一句话} |
| 修改 | N | {一句话} |
| 删除 | N | {一句话} |

---

## 一、新增内容

### 1.1 {新增项1标题}

{新增的功能描述、用户场景、验收标准等，格式与标准 PRD 对应章节一致}

### 1.2 {新增项2标题}

{内容}

---

## 二、修改内容

### 2.1 {修改项1标题}

> 原 PRD §{章节号} "{原章节标题}"

**变更前**：
{原有内容摘要}

**变更后**：
{修改后内容}

**变更原因**：{一句话}

### 2.2 {修改项2标题}

> 原 PRD §{章节号} "{原章节标题}"

**变更前**：
{原有内容摘要}

**变更后**：
{修改后内容}

**变更原因**：{一句话}

---

## 三、删除内容

### 3.1 {删除项1标题}

> 原 PRD §{章节号} "{原章节标题}"

**删除内容**：{被删除的内容摘要}

**删除原因**：{一句话}

---

## 四、Plan 拆分影响

> 本次变更是否影响了原 PRD 的 Plan 拆分建议？必填——增量改动可能改变 Plan 链路。
>
> **模板选择**：| 任务特征 | 推荐模板 | 特征判断 |
> |---------|---------|------|
> | 单一聚焦改动（≤3 文件、无子模块、无新架构） | `simple-plan-template.md` | 配置收敛、重构、修复、单文件新增 |
> | 多模块/跨系统/含架构设计或外部 API | `standard-plan-template.md` | 新功能模块、数据模型变更、跨仓库集成 |

**影响判定**：无影响 / 新增 Plan / 调整 Plan 依赖 / 废弃 Plan

**变更后的 Plan 链路**：

```text
{更新后的 Plan 链路，每个 Plan 标注 模板: {simple/standard}，格式与标准 PRD §10.1 一致}
```

---

## 五、集中裁决层影响

> 本次变更涉及哪些裁决规则的增删改？必填。

| 操作 | 规则 | 裁决条目 | 说明 |
|------|------|------|------|
| 新增 / 修改 / 删除 | {规则描述} | `[[caijue]] id="{entry-id}"` | {一句话} |

---

## 六、关联文档

| 类型 | 路径 | 说明 |
|------|------|------|
| 原 PRD | `docs/{项目}/knowledge/00-需求/{原PRD文件名}.md` | 本次更新基于的 PRD |
