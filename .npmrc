# pnpm 11 构建白名单在 pnpm-workspace.yaml 的 allowBuilds 配置（prisma/engines/esbuild/sharp/onnxruntime-node）
# registry 镜像统一走腾讯（本地 nrm 同源；服务器部署直接读此配置，避免官方源拉大包超时）
registry=https://mirrors.tencent.com/npm/

# 大包（onnxruntime-node 217MB）下载容错
fetch-retries=5
fetch-timeout=120000
network-concurrency=4

