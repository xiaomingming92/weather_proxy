# {需求名称} PRD v{版本号}

> PRD 标准模板：用于新建产品需求文档。定义"做什么、为谁做、做到什么程度"。向下游 Plan/Review/Spec 提供唯一真值输入。
> 适用场景：系统级规划 | 功能/模块级需求 | AI 智能体能力定义。
> 修改已有 PRD 请使用 `prd-incremental-template.md`。
> 可选章节（§五 §六 §七 §九）按场景选用，不适用则删除。

**创建时间**: {ISO 时间戳}
**作者**: {作者标识}
**状态**: 草稿 / 评审中 / 已确认

---

## 一、背景与目标

### 1.1 背景

{一句话描述当前面临的问题或机会}

### 1.2 目标

- {目标1}
- {目标2}

### 1.3 范围

| 维度 | 本次范围 | 明确不做 |
|------|---------|---------|
| {维度1} | {做} | {不做} |
| {维度2} | {做} | {不做} |

---

## 二、目标用户与场景

### 2.1 用户角色

| 角色 | 描述 | 核心诉求 |
|------|------|---------|
| {角色1} | {一句话} | {诉求} |
| {角色2} | {一句话} | {诉求} |

### 2.2 核心场景

| 场景 | 触发条件 | 预期行为 | 优先级 |
|------|---------|---------|:--:|
| {场景1} | {条件} | {行为} | P0 |
| {场景2} | {条件} | {行为} | P1 |

---

## 三、功能需求

### 3.1 功能清单

| 编号 | 功能 | 描述 | 优先级 |
|:----|------|------|:--:|
| F1 | {功能名} | {一句话描述} | P0 |
| F2 | {功能名} | {一句话描述} | P1 |

### 3.2 功能详述

#### F1: {功能名称}

- **输入**: {用户输入/系统输入}
- **处理逻辑**: {核心流程}
- **输出**: {预期产出}
- **交互说明**: {UI 交互要点，无 UI 则写"无"}

#### F2: {功能名称}

- **输入**: {用户输入/系统输入}
- **处理逻辑**: {核心流程}
- **输出**: {预期产出}
- **交互说明**: {UI 交互要点}

---

## 四、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | {如：页面加载 < 2s} |
| 安全 | {如：所有 API 需认证} |
| 可用性 | {如：支持主流浏览器} |
| 数据 | {如：操作日志保留 90 天} |

---

## 五、智能体能力契约（仅 AI 智能体 PRD 时使用）

> 本章节定义智能体的能力边界和裁决逻辑。非智能体需求可删除。

### 5.1 能力清单

| 能力 | 描述 | 触发条件 | 输出格式 |
|------|------|---------|---------|
| {能力1} | {描述} | {何时触发} | {输出 schema} |

### 5.2 裁决规则

当多个能力冲突时，按以下优先级裁决：

1. {规则1}
2. {规则2}

### 5.3 能力边界

- **能做**: {范围}
- **不能做**: {边界约束}
- **降级策略**: {能力不可用时的 fallback}

---

## 六、系统架构约束（仅系统级规划时使用）

> 本章节定义顶层架构的硬约束。功能级需求可删除。

### 6.1 子系统划分

| 子系统 | 职责 | 边界 |
|--------|------|------|
| {子系统1} | {职责} | {边界} |

### 6.2 技术选型约束

| 层级 | 选型 | 约束条件 |
|------|------|---------|
| {前端/后端/数据} | {技术} | {条件} |

---

## 七、接口与数据（涉及 API / 数据模型时使用）

> 本章节定义关键接口契约和数据模型。不涉及可删除。

### 7.1 关键接口

| 接口 | 方法 | 路径 | 说明 |
|------|:--:|------|------|
| {接口名} | GET/POST | `/api/...` | {说明} |

### 7.2 核心数据实体

| 实体 | 核心字段 | 关系 |
|------|---------|------|
| {实体1} | {字段列表} | {关联} |

---

## 八、验收标准

- [ ] {验收条件1}
- [ ] {验收条件2}
- [ ] {验收条件3}

---

## 九、MVP 范围与迭代计划

> 单次交付无需拆分时，写"本次全部交付"即可。需分阶段时填写。

| 阶段 | 范围 | 交付物 | 预计工期 |
|:----|------|------|------|
| Phase 1 核心 | {核心功能集合} | {交付物} | {周数} |
| Phase 2 增强 | {增强功能} | {交付物} | {周数} |

---

## 十、Plan 拆分建议

> 本章节指导下游 AI 如何将本 PRD 拆分为 Plan。必填——没有这条，AI 拿到 PRD 不知道从哪下手。
>
> **模板选择**：| 任务特征 | 推荐模板 | 特征判断 |
> |---------|---------|------|
> | 单一聚焦改动（≤3 文件、无子模块、无新架构） | `simple-plan-template.md` | 配置收敛、重构、修复、单文件新增 |
> | 多模块/跨系统/含架构设计或外部 API | `standard-plan-template.md` | 新功能模块、数据模型变更、跨仓库集成 |

### 10.1 推荐 Plan 链路

```text
Plan 1: {plan名称} ── 模板: {simple/standard} ── 聚焦: {子功能描述}
            │  依赖: 无
            ▼
Plan 2: {plan名称} ── 模板: {simple/standard} ── 聚焦: {子功能描述}
            │  依赖: Plan 1 完成的 {产出物}
            ▼
Plan 3: {plan名称} ── 模板: {simple/standard} ── 聚焦: {子功能描述}
            依赖: Plan 2 完成的 {产出物}
```

### 10.2 Plan 间依赖说明

| 依赖关系 | 原因 |
|---------|------|
| Plan 2 依赖 Plan 1 | {为什么必须先做 Plan 1} |
| Plan 3 依赖 Plan 2 | {为什么必须先做 Plan 2} |

---

## 十一、集中裁决层关联

> 本章节列出本 PRD 涉及的规则和边界，哪些建议集中到 `caijue.toml` 管理。必填——PRD 中的约束不进入集中裁决层，等于没有被强制执行。

### 11.1 建议集中裁决的规则

| 规则 | 类型 | 建议裁决条目 | 说明 |
|------|------|------|------|
| {规则描述} | edge / strategy / contract | `[[caijue]] id="{entry-id}"` | {一句话} |

### 11.2 边界约束

| 约束 | 说明 |
|------|------|
| {边界1} | {如：禁止跨 Expert 共享 Collection} |
| {边界2} | {如：单次检索 topK 上限 20} |

---

## 十二、关联文档

| 类型 | 路径 | 说明 |
|------|------|------|
| 相关需求 | `docs/{项目}/knowledge/00-需求/{关联PRD}.md` | {与本文档的关系} |
